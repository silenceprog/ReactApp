// import { useState } from 'react';
// import { data } from '../shared/projectData';

// export const useSortDate = (data = []) => {

//     const [isDescSort, setIsDescSort] = useState(false);

//     sortByDate = () => {
//         const sortableDate =  [...this.state.blogPosts];
//         sortableDate.sort((a, b) => {
//             if(new Date(b.date) > new Date(a.date)) return isDescSort ? 1 : -1;
//             if (new Date(b.date) < new Date(a.date)) return isDescSort ? -1 : 1;

//             return 0;
//         })

//         return sortableDate;

//     }

//         return{
//             sortableDate,
//             isDescSort,
//             setIsDescSort
//         }
    
// }